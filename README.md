# frogbot-nuget-cpm-repro

E2E fixtures for jfrog/jfrog-cli-security#888 (NuGet Central Package Management auto-fix).
Each branch is one real, restorable CPM repo shape. `main` is intentionally empty.
